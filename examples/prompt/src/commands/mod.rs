/// Asks the user to confirm before a step that cannot be undone
mod confirm;
/// Asks the user to select one of several options
mod select;
/// Asks the user for one line of text
mod text;

clawless::commands!();
