Switch back to main after a feature branch has been merged. Save the current
branch name, checkout main, pull the latest changes, and delete the feature
branch locally. If the current branch is already main, say so and do nothing.
